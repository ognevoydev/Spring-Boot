database:
  username: root
  password: root

spring:
  database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:quisell;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
  jpa.properties.hibernate:
    show_sql: true
    format_sql: true